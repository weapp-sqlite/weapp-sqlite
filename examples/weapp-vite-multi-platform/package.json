{
  "name": "weapp-sqlite-demo-weapp-vite",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "weapp-vite multiPlatform SQLite demo",
  "scripts": {
    "dev": "wv dev -p weapp",
    "dev:web": "wv dev -p web --host",
    "build": "wv build -p weapp",
    "build:weapp": "wv build -p weapp",
    "build:alipay": "wv build -p alipay",
    "build:tt": "wv build -p tt",
    "build:swan": "wv build -p swan",
    "build:jd": "wv build -p jd",
    "build:xhs": "wv build -p xhs",
    "build:web": "wv build -p web",
    "test": "vitest run test/index.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "postinstall": "wv prepare"
  },
  "dependencies": {
    "@weapp-sqlite/demo-shared": "workspace:*",
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "weapp-vite": "^6.22.0"
  }
}
